name: Deploy Docs

on:
  push:
    branches:
      - main
    paths:
      - 'docs/**'
      - '.github/workflows/docs.yml'

jobs:
  call-central-workflow:
    uses: ekke-edu/cloud-computing/.github/workflows/call-central-workflow.yml@main
    with:
      workflow: deploy-docs